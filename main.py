from agent import train

# I cannot get an input variable to work with pygame
# show_train=True will play the training of the agent via pygame and graph the outcomes
# show_train=False will not display the training and just print outputs of games to the terminal
train(show_train=True)
